/**
 * Repository exports
 *
 * Note: TypeScript interfaces (INetworkPlanRepository, SavedPlan) should be
 * imported directly from './network-plan.repository.js' where needed.
 * Barrel exports should only contain runtime code (classes, functions, constants).
 */

export { FileSystemRepository } from './file-system.repository.js';
