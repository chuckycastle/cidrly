#!/usr/bin/env bash
# Git pre-push hook
# Automatically runs verification checks before allowing a push

echo ""
echo "Running pre-push checks..."
echo ""

# Run the verification script
./scripts/verify-pre-push.sh

# Exit with the same status as the verification script
exit $?
